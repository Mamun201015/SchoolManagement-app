import 'package:flutter/material.dart';

class UserSelectView extends StatelessWidget {
  const UserSelectView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("userSelectPage")),
    );
  }
}